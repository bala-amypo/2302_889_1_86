spring.application.name=demo
# Don't change the port
server.port = 9001
# for https
server.forward-headers-strategy=framework 

spring.datasource.url=jdbc:mysql://localhost:3306/transport_pro?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# spring.application.name=demo
# # Don't change the port
# server.port=9001
# # for https
# server.forward-headers-strategy=framework 

# # ADD THESE JWT PROPERTIES (fixes base64 decoding errors)
# jwt.secret=mySecretKeyThatIsLongEnoughForHMACSHA256AtLeast32CharsLongerIsBetter
# jwt.expiration=3600000

# # CHANGE THIS - use test DB (your tests expect this)
# spring.datasource.url=jdbc:mysql://localhost:3306/demo?createDatabaseIfNotExist=true&useSSL=false
# spring.datasource.username=root
# spring.datasource.password=Amypo
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # ADD TEST DATABASE CONFIGURATION (for H2 in-memory DB during tests)
# spring.datasource.hikari.maximum-pool-size=10

# # JPA settings
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# # ADD FOR TESTS (disables HTTPS/SSL issues in tests)
# spring.security.oauth2.resourceserver.jwt.issuer-uri=
# server.ssl.enabled=false
